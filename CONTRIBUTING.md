Thanks for contributing! Please follow these simple rules:

- Fork the repo and create feature branches for changes.
- Keep commits small and focused, add clear commit messages.
- Run linting/tests locally before creating a PR.
- Open a PR against `main` and request a review.

If you need to add images, place them in `public/` and reference them by path. Do not commit large binaries without approval.
